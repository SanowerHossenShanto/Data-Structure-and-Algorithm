#include <stdio.h>void mergeSort(int[], int, int);void merge(int[], int, int, int);int main() {    int x[20], i, n;    printf("Enter the number of elements to be sorted: ");    scanf("%d", &n);    printf("Enter %d elements: ", n);    for (i = 0; i < n; i++)        scanf("%d", &x[i]);    mergeSort(x, 0, n - 1);    printf("\nThe sorted array is:\n");    for (i = 0; i < n; i++)        printf("%4d", x[i]);    return 0;}void mergeSort(int a[], int p, int r) {    if (p < r) {        int q = (p + r) / 2;        mergeSort(a, p, q);        mergeSort(a, q + 1, r);        merge(a, p, q, r);    }}void merge(int a[], int p, int q, int r) {    int b[20], i, l1 = p, r1 = q + 1;    for (i = p; l1 <= q && r1 <= r; i++) {        if (a[l1] < a[r1])            b[i] = a[l1++];        else            b[i] = a[r1++];    }    while (l1 <= q)        b[i++] = a[l1++];    while (r1 <= r)        b[i++] = a[r1++];    for (i = p; i <= r; i++)        a[i] = b[i];}