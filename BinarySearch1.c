#include <stdio.h>int binarySearch(int arr[], int size, int target) {    int low = 0, high = size - 1, mid;    while (low <= high) {        mid = low + (high - low) / 2;        if (target == arr[mid])            return mid;        else if (target < arr[mid])            high = mid - 1;        else            low = mid + 1;    }    return -1;}int main() {    int arr[20], i, numElements, position, key;    printf("\nEnter the number of elements: ");    scanf("%d", &numElements);    printf("\nEnter %d elements in ascending order: ", numElements);    for (i = 0; i < numElements; i++)        scanf("%d", &arr[i]);    printf("\nEnter the element to be searched: ");    scanf("%d", &key);    position = binarySearch(arr, numElements, key);    if (position == -1)        printf("\nThe search is unsuccessful.\n");    else        printf("\n%d is found at location %d.\n", key, position + 1);    return 0;}