#include <stdio.h>#include <stdlib.h>#define MAXSTK 100int top = -1;int items[MAXSTK];int isEmpty();int isFull();void push(int);int pop();void display();int main() {    int x;    char ch = '1';    while (ch != '4') {        printf("\n1-PUSH");        printf("\n2-POP");        printf("\n3-DISPLAY");        printf("\n4-QUIT");        printf("\nEnter your choice: ");        fflush(stdin);        ch = getchar();        switch(ch) {            case '1':                printf("\nEnter the element to be pushed: ");                scanf("%d", &x);                push(x);                break;            case '2':                x = pop();                printf("\nPop element is %d\n", x);                break;            case '3':                display();                break;            case '4':                break;            default:                printf("\nWrong choice! Try again.\n");        }    }    return 0;}int isEmpty() {    return (top == -1);}int isFull() {    return (top == MAXSTK - 1);}void push(int x) {    if (isFull()) {        printf("\nStack full\n");        return;    }    top++;    items[top] = x;}int pop() {    if (isEmpty()) {        printf("\nStack empty\n");        exit(0);    }    int x = items[top];    top--;    return x;}void display() {    if (isEmpty()) {        printf("\nStack empty\n");        return;    }    printf("\nElements in the Stack are:\n");    for (int i = top; i >= 0; i--)        printf("%d\n", items[i]);}